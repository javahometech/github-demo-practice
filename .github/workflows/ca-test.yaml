name: Composite Action Test
on: push
jobs:
  composite-test:
    runs-on: ubuntu-latest
    steps:
      - name: Call composite Action
        uses: javahometech/gha-ca-practice
        with:
          name: "Hari kammana"